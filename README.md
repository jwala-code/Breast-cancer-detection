# Breast-cancer-detection with BST-backed hyperparameter optimisation
A complete data science pipeline that runs a grid search on tumour cell measurements, stores every trial result in a Binary Search Tree, and analyses which model configurations transfer to a second dataset.
